name: Build and Publish

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - run: chmod +x gradlew

      - run: ./gradlew build

      - uses: Kira-NT/mc-publish@v3
        with:
          modrinth-id: U8403aUi
          modrinth-token: ${{ secrets.MODRINTH_TOKEN }}
          github-token: ${{ secrets.GITHUB_TOKEN }}

          files: neoforge/build/libs/!(*-@(shadow|sources)).jar

          dependencies: "emi@1.1.24+1.21.1+neoforge(required){modrinth:fRiHVvU7}"

          game-versions: 1.21.1

      - uses: Kira-NT/mc-publish@v3
        with:
          modrinth-id: U8403aUi
          modrinth-token: ${{ secrets.MODRINTH_TOKEN }}
          github-token: ${{ secrets.GITHUB_TOKEN }}

          files: fabric/build/libs/!(*-@(shadow|sources)).jar

          dependencies: |
            "emi@1.1.24+1.21.1+fabric(required){modrinth:fRiHVvU7}",
            "fabric@0.116.17+1.21.1(required){modrinth:P7dR8mSH}"

          game-versions: 1.21.1
